# Claude Code 配置 Context 消耗审计

> 审计日期：2026-06-21
> 范围：用户级 `~/.claude/`、项目级 `D:\projects\github\origin\apps\fifa2026\.claude\`
> 关联计划：[2026-06-21-context-optimization-plan.md](./2026-06-21-context-optimization-plan.md)

## 1. Agents 层（用户确认：不是问题）

```
~/.claude/agents/                   4 个文件   ← 真正被索引/可用
~/.claude/agency-agents-dev/        45 个文件  ← 未注册，不进 context
~/.claude/agency-agents-reference/  191 个文件 ← 未注册，不进 context
```

只有 `~/.claude/agents/*.md`（`code-reviewer` / `git-workflow-master` /
`minimal-change-engineer` / `project-manager-senior`）被 harness 索引。其余
236 个文件是孤立目录，不消耗 context。**结论：不需要处理。**

## 2. Plugin 层（官方数字，来自 `claude plugin details`）

| Plugin | 状态 | Always-on token |
|---|---|---:|
| superpowers | ✔ 启用 | ~482 |
| claude-md-management | ✔ 启用 | ~121 |
| frontend-design | ✔ 启用 | ~54 |
| session-report | ✔ 启用 | ~48 |
| context7 | ✔ 启用 | ~0（纯 MCP，schema 运行时解析） |
| claude-hud | ✔ 启用 | ~0（纯 statusline） |
| skill-creator | ✘ 已禁用 | 0（禁用前 75） |
| andrej-karpathy-skills | ✘ 已禁用 | 0（禁用前 64） |
| plugin-dev | ✘ 已禁用 | 0（禁用前 **1,566**，最贵的一个） |
| github | ✘ 已禁用 | 0 |

**已启用 plugin 合计 ~705 token。** 最贵的 `plugin-dev`（1,566 tok）已经被禁用。
**结论：plugin 层已经是优化过的状态，不需要再处理。**

## 3. 常驻文档层（真正的优化目标）

| 文件 | 行数 | 字节 | 估算 token | 备注 |
|---|---:|---:|---:|---|
| `~/.claude/CLAUDE.md` | 137 | 6,211 | ~1,900 | 含 §7 skill 触发规则、§8 角色分工表 |
| `~/.claude/rules/plugins.md`（@import） | 47 | 2,365 | ~750 | 含详细原则/边界/操作说明，对模型冗余 |
| 项目 `CLAUDE.md` | 64 | 4,965 | ~1,400 | §"角色分工" 与用户级 §8 重复 |
| 项目 `.claude/rules/vibe-workflow.md`（@import） | 112 | 5,829 | ~1,700 | 完整多步流程，符合"长流程→转 skill"标准 |
| harness 自动 memory 索引 `MEMORY.md` | 8 | — | ~150 | 已经是索引化，正常 |

**估算口径**：中文按 ~1.5–2 字符/token，英文/代码按 ~4 字符/token，均为粗估非精确值。

**当前估算常驻总量 ≈ 6,800 token**（不含内置 skill 元数据和 MCP 指令的小额开销）。

## 4. 不进 context 的部分（确认无需处理）

- `~/.claude/settings.json`、`~/.claude.json` — 运行时配置/统计，不进模型 context
- `.claude/hooks/*.sh` — harness 执行，不进 context
- `.claude/session-log.md`（271 行）— 磁盘审计文件
- 项目仓库内 `memory/*.md`（`external_resources.md` + `user_preferences.md`）— 按需读取
- harness 自动 memory 的 7 个 topic 文件 — 按需读取

## 5. 新发现：两套并行的 memory 系统

1. 项目仓库内 `memory/`（手动维护，被项目 `CLAUDE.md` "Session Continuity" 引用）
2. harness 自动 memory（`~/.claude/projects/D--.../memory/`，索引自动注入，topic 文件按需）

两者职责重叠（跨会话事实/偏好），目前无交叉引用，存在未来内容分裂的风险。优先级：中。

## 6. claude CLI 能力备注

- `claude plugin list` / `claude plugin details <name>` 可在 shell 中直接运行，给出权威 token 估算 —— 已采用其数据替代手工估算。
- `claude mcp --help` 确认项目内无 `.mcp.json`，全局 `~/.claude.json` 中所有项目的 `mcpServers` 均为 `{}`。
- `/context`、`/memory`、`/agents` 等 slash command 是交互式 REPL 命令，无法通过非交互 Bash 调用，因此本次审计改用文件统计 + `claude plugin` 子命令完成。

## 7. 优化后对比（Task 1-5 执行完毕，2026-06-22 实测）

| 项目 | 优化前 | 优化后 | 变化 | 说明 |
|---|---:|---:|---:|---|
| 用户 `CLAUDE.md` 行数 | 137 | 137 | 0 | 本轮未处理（计划中标为低优先级，留待后续） |
| 用户 `rules/plugins.md` 行数 | 47 | 18 | -29 | 详细原则/边界/操作说明迁至 `~/.claude/archive/plugins-rationale.md`，按需查阅 |
| 项目 `CLAUDE.md` 行数 | 64 | 51 | -13 | 角色分工表去重（Task 3）+ vibe-workflow 引用方式调整（Task 4）+ memory 分工说明（Task 5，+3行） |
| 项目常驻 `rules/` 行数 | 112（vibe-workflow.md） | 0 | -112 | 已转为 `.claude/skills/vibe-workflow/SKILL.md`，按需触发，不再 `@import` 常驻 |
| 已启用 plugin token | ~705 | ~705 | 0 | 本轮未涉及 plugin 启停 |
| harness 自动 memory 索引行数 | 8 | 8 | 0 | 未改动，保持索引化 |
| **估算常驻 token 总量** | **~6,800** | **~4,000** | **~-2,800（约 -41%）** | 主因：vibe-workflow 转 skill（-1,700）+ plugins.md 精简（-450）+ 项目 CLAUDE.md 去重（-300） |

**追加（2026-06-22）：用户级 `~/.claude/CLAUDE.md` 压缩**

应用户要求，把原计划标注为"低优先级"的这一项也执行了，但改用了比原计划更彻底的方式：不是简单 `@import` 拆分（那样内容仍会全量常驻，不省 token），而是直接压缩措辞、合并 §7 五个小节为一张表，137→**78 行**，规则关键词逐条复核无遗漏，完整原文存档至 `~/.claude/archive/claude-md-full-v1-20260621.md`（commit `a4cf4ae`）。

| 项目 | 优化前 | 优化后 | 变化 |
|---|---:|---:|---:|
| 用户 `CLAUDE.md` 行数 | 137 | 78 | -59 |
| 估算常驻 token（用户 CLAUDE.md） | ~1,900 | ~1,100 | ~-800 |

**累计估算常驻 token 总量：~6,800 → ~3,200，约 -53%**

**未处理项：** 无（原计划全部 6 项 + 追加项均已执行）。

**衍生事项（执行过程中发现，已分别处理或留待后续）：**
- 项目级 `.claude-backup/` 已加入 `.gitignore`（commit `30d2694`），避免误提交配置备份。
- vibe-workflow §1/§2 的 `gh issue create` 步骤与项目实际"未装 `gh`"长期矛盾；**2026-06-22 已解决**——`gh` v2.95.0 安装并完成 `gh auth login`（账号 `m42981454-tech`），`gh issue list`/`gh repo view` 验证可用。`CLAUDE.md`/`progress.md` 中"`gh` 未安装"的过时记录已同步更新（commit `6f50a00`）。SKILL.md 正文的 `gh issue create` 命令现在是真实可执行的，未再改动。
- 两套并行 memory 系统（仓库内 `memory/` vs harness 自动 memory）已在项目 CLAUDE.md "Session Continuity" 节补充分工说明（commit `60221e8`），未做内容搬移，仅澄清职责边界。

**本次优化全部 commit（feature/dev 分支）：**
```
30d2694 chore: .claude-backup/ 加入 gitignore，避免误提交配置备份
e0608fe chore(claude-config): 精简 plugins.md，详细说明迁至 archive 按需查阅
26483c2 chore(claude-config): 角色分工表去重，单一来源指向用户级 CLAUDE.md §8
fa85ceb refactor(claude-config): vibe-workflow 规则转为按需触发 skill，不再常驻加载
60221e8 docs(claude-config): 澄清仓库内 memory/ 与 harness 自动 memory 的分工边界
```
