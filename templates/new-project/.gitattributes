# Keep shell entrypoints executable and LF-only on Windows.
*.sh text eol=lf
.githooks/* text eol=lf
project.env text eol=lf

# PowerShell scripts are friendlier with CRLF on Windows.
*.ps1 text eol=crlf
